Action()
{
	// scripted added to the 'myLibrary' git project.
	// This approach will need to ignore the built in integrations of Vugen and just use native
	// cli or GitHub Desktop to perform commits, pull, push, etc.
	
	// This line is for the dev branch which will be merged into main when ready.
	return 0;
}
